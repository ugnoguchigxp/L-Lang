# エージェント向け指示

- プロジェクトでの作業開始時に一度だけ、利用可能なら `initial_instructions` MCP ツールを実行する。個別タスクごとには繰り返さない。
- ユーザーが明示的に依頼しない限り、既存の別 Codex タスク／スレッドへプロンプトやメッセージを送信しない。

## Binaryen の情報取得

Wasm のコード生成、最適化設定、性能・メモリ調査、Binaryen 更新を扱う場合は、利用可能なら `$llang-binaryen` Skill を使う。Skill が未登録の環境では[Binaryen 案内](docs/agent-context/binaryen/README.md)から作業に合う資料を一つ選ぶ。追加情報は必要になった時点で読み、無関係な作業で全資料を読み込まない。

資料は生成・実装を助ける知識であり、言語仕様や ABI の変更を許可するものではない。実験用の最適化を製品の既定動作と混同しない。
