# module-value-v1 order-line example

The entry function validates one order line, calculates its integer amount, and returns a tagged success or business-error value. Run it with:

```sh
bun run llang module lint application/quote.llang.jsonc --root examples/module-order-line --entry quote --profile module-value-v1
```
