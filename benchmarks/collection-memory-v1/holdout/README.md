# Holdout

This directory is intentionally empty. A workload owner must freeze a separate
holdout before making performance claims beyond the checked-in regression
fixture. Results from `expected/cost.json` do not populate this directory.
